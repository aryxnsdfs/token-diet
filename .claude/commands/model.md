---
description: Switch to a cheaper or local AI model
argument-hint: <tier>
---

Run the ctx `model` command via the MCP server (tool `ctx:model`) with argument: $ARGUMENTS.

This invokes the engine handler for `/model` and injects the result into context.

---
description: Summarize old chat to free up space
---

Run the ctx `cleanup` command via the MCP server (tool `ctx:cleanup`).

This invokes the engine handler for `/cleanup` and injects the result into context.

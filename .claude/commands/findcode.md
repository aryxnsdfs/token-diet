---
description: Find one function or class and show its code
argument-hint: <symbol>
---

Run the ctx `findcode` command via the MCP server (tool `ctx:findcode`) with argument: $ARGUMENTS.

This invokes the engine handler for `/findcode` and injects the result into context.

---
description: Open a file — pull it into the chat
argument-hint: <file|symbol>
---

Run the ctx `openfile` command via the MCP server (tool `ctx:openfile`) with argument: $ARGUMENTS.

This invokes the engine handler for `/openfile` and injects the result into context.

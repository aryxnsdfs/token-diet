---
description: Apply the AI's code changes to your files
---

Run the ctx `applychanges` command via the MCP server (tool `ctx:applychanges`).

This invokes the engine handler for `/applychanges` and injects the result into context.

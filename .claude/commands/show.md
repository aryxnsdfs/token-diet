---
description: Pull a specific file or function into the chat
argument-hint: <file|symbol>
---

Run the ctx `show` command via the MCP server (tool `ctx:show`) with argument: $ARGUMENTS.

This invokes the engine handler for `/show` and injects the result into context.

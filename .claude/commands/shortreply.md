---
description: Keep replies short — AI sends only changed lines
---

Run the ctx `shortreply` command via the MCP server (tool `ctx:shortreply`).

This invokes the engine handler for `/shortreply` and injects the result into context.

---
description: See how many tokens you saved
---

Run the ctx `savings` command via the MCP server (tool `ctx:savings`).

This invokes the engine handler for `/savings` and injects the result into context.

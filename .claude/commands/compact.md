---
description: Make the AI reply with only changed lines
---

Run the ctx `compact` command via the MCP server (tool `ctx:compact`).

This invokes the engine handler for `/compact` and injects the result into context.

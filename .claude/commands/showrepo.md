---
description: Show the whole project structure (ranked by relevance)
argument-hint: [path]
---

Run the ctx `showrepo` command via the MCP server (tool `ctx:showrepo`) with argument: $ARGUMENTS.

This invokes the engine handler for `/showrepo` and injects the result into context.

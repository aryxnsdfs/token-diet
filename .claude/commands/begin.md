---
description: Begin a coding session — scan code, show overview, turn on smart mode
---

Run the ctx `begin` command via the MCP server (tool `ctx:begin`).

This invokes the engine handler for `/begin` and injects the result into context.

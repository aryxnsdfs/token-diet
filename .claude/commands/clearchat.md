---
description: Free up chat space — summarize old messages
---

Run the ctx `clearchat` command via the MCP server (tool `ctx:clearchat`).

This invokes the engine handler for `/clearchat` and injects the result into context.

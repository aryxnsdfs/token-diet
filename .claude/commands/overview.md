---
description: Show your project structure (ranked by relevance)
argument-hint: [path]
---

Run the ctx `overview` command via the MCP server (tool `ctx:overview`) with argument: $ARGUMENTS.

This invokes the engine handler for `/overview` and injects the result into context.

---
description: Switch AI models (cheaper or local)
argument-hint: <tier>
---

Run the ctx `changeai` command via the MCP server (tool `ctx:changeai`) with argument: $ARGUMENTS.

This invokes the engine handler for `/changeai` and injects the result into context.

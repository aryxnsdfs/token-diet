---
description: Apply code changes from the AI's last reply
---

Run the ctx `patch` command via the MCP server (tool `ctx:patch`).

This invokes the engine handler for `/patch` and injects the result into context.

---
description: Show token and cost savings
---

Run the ctx `showstats` command via the MCP server (tool `ctx:showstats`).

This invokes the engine handler for `/showstats` and injects the result into context.

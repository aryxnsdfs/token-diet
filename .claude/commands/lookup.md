---
description: Show the code of one function or class
argument-hint: <symbol>
---

Run the ctx `lookup` command via the MCP server (tool `ctx:lookup`) with argument: $ARGUMENTS.

This invokes the engine handler for `/lookup` and injects the result into context.
